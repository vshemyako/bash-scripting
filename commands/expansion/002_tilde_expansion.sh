#!/usr/bin/env bash

# Tilde is expanded to the home directory of a logged-in user
echo 'My home directory is' ~

# It's possible to output other users' home directories
echo 'Home directory of a root user is' ~root